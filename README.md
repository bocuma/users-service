# Users service

Simple AUTH API

## To run

From the `server` directory: 

`cabal install`

`cabal run`

## To run the tests

From the `server` directory: 

`cabal sandbox init`

`cabal install --enable-tests`

`cabal test`


